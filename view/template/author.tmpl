{{define "content"}}
{{with .Author}}
<div class="page-header">
   <h1>
      {{.User.Username}}
   </h1>
</div>
<div id="posts" style="float:left;">
   {{range .Posts}}
   <div class="row-fluid">
      <div class="span2">
         <img src="{{.ImageURL}}" height="100px" width="100px"></img>
      </div>
      <div class="span10 page-header">
         <h2>
            <a href="/post/{{.Id}}">
               {{.Title}}
            </a>
            <p><small>Posted on {{.Date.Weekday}} {{.Date.Day}} {{.Date.Month}} {{.Date.Year}}</small></p>
         </h2>
      </div>
   </div>
   {{else}}
   <div class="hero-unit"><h1>There are not post on this blog!</h1></div>
   {{end}}
</div>

<div id="comments" style="float:right;">
   {{range .User.Comments}}
   <h4>Comments</h4>
   <blockquote id="{{.Id}}">
      <p>{{.ContentMarkdown}}</p>
      <small><a href="/user/{{.User.Id}}">{{.User.Username}}</a>, at
         {{.Date.Hour}}h{{.Date.Minute}} on
         {{.Date.Weekday}} {{.Date.Day}} {{.Date.Month}} {{.Date.Year}},
         said:
      </small>
   </blockquote>
   {{else}}
   <h4>No comments</h4>
   Ita, quae mutat, ea corrumpit, quae sequitur sunt tota Democriti, atomi, inane, imagines.
   {{end}}
</div>
{{end}}
{{end}}
